rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Standard Firebase-authenticated access; no member document is needed.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
